{
  "name": "atom-score",
  "main": "./lib/atom-score",
  "version": "0.0.0",
  "private": true,
  "description": "Add a score keeping system for workflow training",
  "keywords": [],
  "activationCommands": {},
  "repository": "https://github.com/MrRacoon/atom-score",
  "license": "MIT",
  "scripts": {
    "lint": "npm run lint:lib && npm run lint:spec",
    "lint:lib": "eslint ./lib/**",
    "lint:spec": "eslint ./spec/**",
    "spec": "atom --test --timeout 60 ./spec/**/*-spec.js",
    "test": "npm run lint && npm run spec"
  },
  "engines": {
    "atom": ">=1.21.0 <2.0.0"
  },
  "dependencies": {
    "chance": "^1.0.4",
    "d3": "^4.2.2",
    "ramda": "^0.23.0",
    "redux": "^3.6.0",
    "reredeux": "^0.1.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^4.18.2",
    "eslint-config-google": "^0.7.1"
  }
}
