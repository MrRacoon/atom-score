'use babel'

import * as history from './history'
import * as score from './score'
import * as counter from './counter'
import * as last from './last'
import * as recent from './recent'

export default { counter, history, score, last, recent }
